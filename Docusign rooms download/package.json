{
  "name": "docusign-rooms-mass-exporter",
  "private": true,
  "version": "2.0.0",
  "description": "Automated tests for the Docusign Rooms Mass Exporter Chrome extension's pure-logic functions. Not part of the loaded extension itself - Chrome only ever reads manifest.json and the files it references.",
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  }
}
